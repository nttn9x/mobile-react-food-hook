import { StyleSheet } from "react-native";
export default StyleSheet.create({
  layoutFlexColumnCenter: {
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center"
  },
  layoutFlexRowCenter: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center"
  }
});
